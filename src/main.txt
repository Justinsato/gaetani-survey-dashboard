import React from 'react';
import ReactDOM from 'react-dom/client';
import CombinedGaetaniSurveyAnalysis from './CombinedGaetaniSurveyAnalysis';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CombinedGaetaniSurveyAnalysis />
  </React.StrictMode>,
);